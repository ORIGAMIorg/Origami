"use client"
import { Layout } from "@/lib/antd";

export default function Header() {
  return (
    <Layout.Header
      style={{
        padding: "0 16px",
        background: '#fff',
      }}
    >
      HR Dashboard
    </Layout.Header>
  );
}
